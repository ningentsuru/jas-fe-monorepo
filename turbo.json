{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*", "tsconfig.json", "src/**", "app/**", "pages/**"],
      "env": ["ENABLE_EXPERIMENTAL_COREPACK", "NODE_ENV"],
      "outputs": [
        ".next/**",
        "!.next/cache/**",
        ".nuxt/**",
        ".output/**",
        "dist/**",
        "storybook-static/**"
      ]
    },
    "lint": {
      "inputs": ["$TURBO_DEFAULT$"]
    },
    "check-types": {
      "inputs": ["$TURBO_DEFAULT$", "tsconfig.json", "tsconfig.*.json", "src/**"]
    },
    "test:unit": {
      "inputs": ["$TURBO_DEFAULT$", "vitest.config.ts", "vitest.setup.ts", "src/**"],
      "outputs": ["coverage/**"]
    },
    "test:e2e": {
      "dependsOn": ["build"],
      "inputs": ["$TURBO_DEFAULT$", "playwright.config.ts", "e2e/**"],
      "outputs": ["playwright-report/**", "test-results/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
